type Query {
  statuses: [Status!]!
  priorities: [Priority!]!
  estimatedHour: EstimatedHour!
}
type Status {
  name: ID!
  count: Int!
}

type Priority {
  name: ID!
  count: Int!
}

type EstimatedHour {
  min: Int!
  max: Int!
}
